# Bananas — Static Site

A minimal, no-build static site all about bananas. Open `index.html` to view and edit.

## What's inside
- Simple semantic HTML (no frameworks) with a friendly design
- CSS tuned for a banana theme (soft yellows, readable layout)
- Optional JS hook in `assets/script.js` for future enhancements

## Deploy
Use the Preview on the toolbar to deploy. To customize the URL for static sites, adjust your deployment settings under Project Settings.

Generated by Genie at https://cosine.sh
